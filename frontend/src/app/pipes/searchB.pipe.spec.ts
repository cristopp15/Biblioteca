import { SearchBPipe } from './searchB.pipe';

describe('SearchBPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBPipe();
    expect(pipe).toBeTruthy();
  });
});

